# Aquora — Product Roadmap (AO-executable PR backlog)

> 46 self-contained PRs across 6 milestones, grounded in the real repo, scoped for autonomous execution by AO (ao-agents.com).
> Legend: effort S/M/L · risk low/med/high · 🔒 = owner-gated (needs a decision/credential).

**Repo:** github.com/ralch22/aquora · git root `/Users/admin/Documents/aquora` (source under `aquora-store/`).
**Live:** storefront rev 00071 (v71), backend v22 · Cloud Run europe-west1 · aquora.ae via Cloudflare Worker.
**Blocking reality:** working tree is ~106 files / 18 commits ahead of origin/main (deploys bypassed git). **WS0-PR1 unblocks everything.**


---
## M0 · HARNESS — make AO possible at all
*Reconcile git ↔ production, PR→CI→deploy pipeline, secret rotation, AO config. Nothing else can be AO-run until this lands.*

#### WS0-PR1 — Reconcile the 106 uncommitted files into a clean committed main matching production  `[M/med]`
- **Problem:** git status shows 52 modified + 54 untracked files (106 total) at the repo root /Users/admin/Documents/aquora (the git root is the PARENT of aquora-store/). The live Cloud Run deploys (storefront v71, backend v22) were built straight from this working tree via `gcloud builds submit`, NOT from git, so origin/main is 18 commits behind AND missing all working-tree changes. Until the working tree == a committed, pushed main, no PR→CI pipeline can deploy what is actually live, and AO agents would branch from a stale base. The untracked set is all real shipped work (reviews/qa Medusa modules + their migrations, compare, wishlist, pool-care/guides/tools pages, mobile-checkout-bar, checkout-steps, banners, recommended-rail, etc.).
- **Scope:** Operate in /Users/admin/Documents/aquora (the actual git root; note files are tracked under the aquora-store/ prefix). Do NOT modify any source content — this is purely a version-control reconciliation so committed==deployed. (1) Audit the 54 untracked paths and 52 modified files; confirm they are intended product code, not scratch. (2) Ensure the root .gitignore keeps excluding regenerable artifacts (node_modules, *.log incl backend.log/seed.log, .next, .medusa, data/catalog-raw.json, data/image-map.json, data/reference/, aquora-store/apps/storefront/.env.production) and NEVER tracks env/secret files. (3) Stage and commit the real source changes in coherent commits (e.g. 'reviews+qa modules', 'compare/wishlist', 'tools/guides pages', 'home rails & banners', 'checkout steps & mobile bar'), keeping the new Medusa migrations under aquora-store/apps/backend/src/modules/review/migrations/ and .../qa/migrations/ in the same commit as their module code. (4) Push to origin/main so GitHub == the live working tree. Because live==working-tree, make NO behavioral edits — only commit what exists.
- **Key files:** /Users/admin/Documents/aquora/.gitignore, /Users/admin/Documents/aquora/aquora-store/apps/backend/src/modules/review/, /Users/admin/Documents/aquora/aquora-store/apps/backend/src/modules/qa/, /Users/admin/Documents/aquora/aquora-store/apps/backend/src/api/store/reviews/, /Users/admin/Documents/aquora/aquora-store/apps/storefront/src/modules/compare/, /Users/admin/Documents/aquora/aquora-store/apps/storefront/src/modules/wishlist/
- **Acceptance:** ✓ `git status` in /Users/admin/Documents/aquora reports a clean working tree (no modified, no untracked source files) after the commits. ✓ `git log origin/main..HEAD` is empty (all local commits, including the prior 18, are pushed to origin/main on github.com/ralch22/aquora). ✓ No .env, .env.production, *.dev.vars, secret, or build-output (.next/.medusa/dist/node_modules) file is tracked; `git ls-files | grep -E '\.env($|\.)|dev.vars'` returns nothing. ✓ The new review and qa module migration files are committed alongside their module source. ✓ A diff of the committed tree vs the pre-reconciliation working tree shows zero source-content changes (only VCS state changed).

#### WS0-PR2 — Add branch protection, PR template, and CODEOWNERS for github.com/ralch22/aquora  `[S/low]` 🔒 · depends: WS0-PR1
- **Problem:** There is no .github/ directory anywhere in the repo and no branch protection. AO agents will open PRs against main, but nothing enforces review, status checks, or a consistent PR description, and no required-check gate exists to stop a broken change from merging once CI is added.
- **Scope:** Create .github/ at the git root (/Users/admin/Documents/aquora/.github). Add: (1) pull_request_template.md capturing the agent runbook essentials — which service(s) changed (backend/storefront/both), whether a Medusa migration was added (and thus needs the out-of-band migration job), the DEFERRED-SUSPENSE invariant checkbox (funnel-critical UI must render INLINE, never in a streamed Suspense), the NO-FABRICATION checkbox (no fake reviews/stock/urgency), and a test/verify section. (2) CODEOWNERS assigning ralch22 as default owner. (3) A short docs note describing the required GitHub branch-protection settings for main (require PR, require the CI status check from WS0-PR3 once it exists, no direct pushes) — and apply them via `gh api` where the agent has permission, otherwise document them for the owner. Configuring the actual GitHub branch-protection rule requires repo-admin rights.
- **Key files:** /Users/admin/Documents/aquora/.github/pull_request_template.md, /Users/admin/Documents/aquora/.github/CODEOWNERS
- **Acceptance:** ✓ .github/pull_request_template.md exists and includes explicit checkboxes for: service(s) touched, migration-added (out-of-band job needed), no streamed-Suspense around funnel UI, no fabricated data. ✓ .github/CODEOWNERS names @ralch22 as the catch-all owner. ✓ A docs file (e.g. .github/BRANCH_PROTECTION.md) specifies the exact main-branch protection settings, and the rule is applied via `gh` if admin token is available; otherwise the file flags it as owner-gated. ✓ Opening a test PR renders the template body automatically.
- **🔒 Owner input:** Owner (ralch22) must confirm/enable the GitHub branch-protection rule on main if the agent's gh token lacks repo-admin scope, and confirm @ralch22 is the correct CODEOWNERS handle.

#### WS0-PR3 — Build PR CI (lint + typecheck + build) via GitHub Actions for backend and storefront  `[M/low]` · depends: WS0-PR1
- **Problem:** There is no CI of any kind. AO-authored PRs can introduce type errors, lint failures, or build breaks that only surface at `gcloud builds submit` (the Dockerfiles run `npm run build`). A merged-PR auto-deploy (WS0-PR4) is unsafe without a gate that proves both apps build first. This is a monorepo (turbo) with apps/backend (Medusa v2) and apps/storefront (Next.js 15) under the aquora-store/ subfolder.
- **Scope:** Add .github/workflows/ci.yml that runs on pull_request and push to main. Use the existing turbo tasks from /Users/admin/Documents/aquora/aquora-store/package.json (lint, build) — note the repo root is the parent, so the workflow must `working-directory: aquora-store` (or set it per-step). Install with `npm install --legacy-peer-deps` to match the Dockerfiles. Run lint and build for both apps; add a path filter so backend-only changes can skip the storefront build and vice-versa if cheap to do, but a full build is acceptable. Do NOT require live DB or secrets — the storefront build needs NEXT_PUBLIC_* placeholders only (provide dummy NEXT_PUBLIC_MEDUSA_BACKEND_URL / publishable key as workflow env so `next build` doesn't fail). Make the resulting check the required status for branch protection (referenced by WS0-PR2).
- **Key files:** /Users/admin/Documents/aquora/.github/workflows/ci.yml, /Users/admin/Documents/aquora/aquora-store/package.json, /Users/admin/Documents/aquora/aquora-store/turbo.json, /Users/admin/Documents/aquora/aquora-store/apps/backend/Dockerfile, /Users/admin/Documents/aquora/aquora-store/apps/storefront/Dockerfile
- **Acceptance:** ✓ ci.yml triggers on pull_request and push:main and runs from the aquora-store working directory. ✓ The workflow runs `npm install --legacy-peer-deps` then turbo lint + build for backend and storefront, and fails the check on any lint/type/build error. ✓ The storefront build step passes using dummy NEXT_PUBLIC_* env (no real secrets), proving the build without leaking credentials. ✓ A PR with a deliberate type error fails the check; a clean PR passes. ✓ The check name is documented so WS0-PR2 can mark it required.

#### WS0-PR4 — Auto-deploy merged main to Cloud Run, mirroring the gcloud build/deploy recipe (path-scoped per service)  `[L/high]` 🔒 · depends: WS0-PR3, WS0-PR6
- **Problem:** Deploys are 100% manual (`gcloud builds submit aquora-store/apps/<svc> --tag .../aquora/<svc>:vN` then `gcloud run deploy aquora-<svc> --image ...:vN`), which is why GitHub drifted from production. AO agents merge PRs but nothing ships them. We need a merge-to-main → build image → deploy the RIGHT Cloud Run service automatically, while preserving the two hard deploy constraints: the storefront needs .env.production present at build (NEXT_PUBLIC_* inlining — already handled by aquora-store/apps/storefront/.gcloudignore), and the backend must NOT run migrations on boot (240s startup-probe limit — migrations are applied out-of-band, see WS0-PR5).
- **Scope:** Add .github/workflows/deploy.yml triggered on push to main (after CI passes). Use a path filter on aquora-store/apps/backend/** vs aquora-store/apps/storefront/** so only the changed service deploys (deploy both if both changed). For each changed service: authenticate to GCP via Workload Identity Federation or a deploy service-account key stored as a GitHub secret (project emerge-digital-web-7034, region europe-west1, Artifact Registry repo 'aquora'); run `gcloud builds submit aquora-store/apps/<svc> --tag europe-west1-docker.pkg.dev/emerge-digital-web-7034/aquora/<svc>:<git-sha> --machine-type=e2-highcpu-8`; then `gcloud run deploy aquora-<svc> --image ...:<git-sha> --region europe-west1` (image-only deploy, which PRESERVES existing env vars + command overrides + Cloud SQL socket). For the storefront, ensure .env.production is materialized at build time from a GitHub secret / Secret Manager (it is gitignored). Tag images with the git SHA (not vN counter) for traceability. Document a rollback (`gcloud run services update-traffic ... --to-revisions`).
- **Key files:** /Users/admin/Documents/aquora/.github/workflows/deploy.yml, /Users/admin/Documents/aquora/aquora-store/apps/backend/Dockerfile, /Users/admin/Documents/aquora/aquora-store/apps/storefront/Dockerfile, /Users/admin/Documents/aquora/aquora-store/apps/storefront/.gcloudignore, /Users/admin/Documents/aquora/HANDOFF.md
- **Acceptance:** ✓ Merging a storefront-only change to main builds + deploys only aquora-storefront; a backend-only change deploys only aquora-backend; a change touching both deploys both. ✓ The deploy uses image-only `gcloud run deploy` so existing env vars, command overrides, and the Cloud SQL socket are preserved (verified by service config diff before/after). ✓ Storefront image is built with .env.production present so NEXT_PUBLIC_* (incl. Stripe publishable key, backend URL) are inlined — checkout does NOT white-screen post-deploy. ✓ Backend deploy does NOT trigger migrations on boot; the service passes its startup probe. ✓ Images are tagged with the commit SHA and a documented `gcloud run services update-traffic` rollback path exists. ✓ GCP auth uses WIF or a secret-stored SA key — no credential is printed in logs.
- **🔒 Owner input:** Owner must provision GCP deploy auth for GitHub Actions: either set up Workload Identity Federation for github.com/ralch22/aquora, or create a deploy service account (roles: Cloud Build Editor, Cloud Run Admin, Artifact Registry Writer, Service Account User) and add its key as a GitHub Actions secret. Confirm project=emerge-digital-web-7034, region=europe-west1, AR repo=aquora, service names aquora-backend/aquora-storefront.

#### WS0-PR5 — Out-of-band migration runner job for backend Medusa module migrations on deploy  `[M/med]` · depends: WS0-PR1, WS0-PR6
- **Problem:** The backend Dockerfile explicitly states migrations are NOT run on boot (a blocking `medusa db:migrate` exceeds Cloud Run's 240s startup probe and backgrounding it starves the single CPU). New module migrations already exist uncommitted (review: Migration20260628084606.ts, qa: Migration20260628103955.ts) and future AO PRs (reviews engine, promotions) will add more. The auto-deploy (WS0-PR4) must apply pending migrations safely AFTER image build but via a short-lived job, not on the serving instance — otherwise new tables never get created and the feature 500s in prod.
- **Scope:** Create a reusable migration mechanism the deploy workflow can invoke when a PR adds/changes a migration. Per the Dockerfile guidance, the reliable path is a short-lived Cloud Run Job (or `gcloud run jobs execute`) attached to the same Cloud SQL instance + DATABASE_URL that runs the generated migration SQL idempotently (CREATE TABLE IF NOT EXISTS style), rather than the slow/hang-prone `medusa db:migrate` CLI. Provide: (1) a small node `pg` runner script under aquora-store/apps/backend (e.g. src/scripts/run-migrations.ts or a standalone pg script) that applies pending module migration SQL idempotently and exits non-zero on failure; (2) a Cloud Run Job definition or `gcloud run jobs` recipe wired to the aquora-db Cloud SQL socket and DATABASE_URL secret; (3) a deploy.yml step that runs it before/after the backend `gcloud run deploy` only when migration files changed in the PR. Reference the existing review/qa migration SQL as the template.
- **Key files:** /Users/admin/Documents/aquora/aquora-store/apps/backend/Dockerfile, /Users/admin/Documents/aquora/aquora-store/apps/backend/src/modules/review/migrations/Migration20260628084606.ts, /Users/admin/Documents/aquora/aquora-store/apps/backend/src/modules/qa/migrations/Migration20260628103955.ts, /Users/admin/Documents/aquora/aquora-store/apps/backend/medusa-config.ts, /Users/admin/Documents/aquora/.github/workflows/deploy.yml
- **Acceptance:** ✓ A migration runner exists that applies pending module migrations idempotently against the aquora-db Cloud SQL instance and exits cleanly (no hang, non-zero on error). ✓ The runner is executed as a short-lived Cloud Run Job / `gcloud run jobs execute`, NOT on the serving aquora-backend instance, so the 240s startup probe is never blocked. ✓ deploy.yml runs the migration step only when the merged PR changed files under any */migrations/ path, before serving the new backend revision the affected tables. ✓ Re-running the runner on an already-migrated DB is a no-op (idempotent) and succeeds. ✓ DATABASE_URL / Cloud SQL credentials are sourced from Secret Manager (WS0-PR6), never hardcoded or printed.

#### WS0-PR6 — Move secrets to Secret Manager, wire Cloud Run to reference them, and rotate the leaked Cloud SQL password  `[L/high]` 🔒 · depends: WS0-PR1
- **Problem:** Secrets are currently set directly as Cloud Run env vars and the Cloud SQL password was leaked once and still needs rotation. The backend reads DATABASE_URL, COOKIE_SECRET, JWT_SECRET, STRIPE_API_KEY, STRIPE_WEBHOOK_SECRET (medusa-config.ts); the storefront build needs NEXT_PUBLIC_* via .env.production (gitignored). For the auto-deploy (WS0-PR4) to be safe and reproducible, these must live in Google Secret Manager and be referenced by the services/build, and the leaked DB password must be rotated. NEVER print any secret/.env file while doing this.
- **Scope:** (1) Create Secret Manager secrets (project emerge-digital-web-7034) for: DATABASE_URL (or the DB password component), COOKIE_SECRET, JWT_SECRET, STRIPE_API_KEY, STRIPE_WEBHOOK_SECRET, and the storefront's NEXT_PUBLIC_* build values used to generate .env.production. (2) Update aquora-backend Cloud Run to reference these via --set-secrets (so env stays out of the service YAML), preserving the Cloud SQL socket. (3) Rotate the Cloud SQL password on instance aquora-db (`gcloud sql users set-password`), update the DATABASE_URL secret to the new password, and redeploy/restart the backend so it reconnects — verify the app still serves. (4) Grant the deploy SA + the backend runtime SA secretAccessor on the relevant secrets. (5) Document the secret inventory and the 'never cat a secret file' rule in the runbook (WS0-PR7). Do all of this without echoing any secret value to logs.
- **Key files:** /Users/admin/Documents/aquora/aquora-store/apps/backend/medusa-config.ts, /Users/admin/Documents/aquora/aquora-store/apps/backend/.env.template, /Users/admin/Documents/aquora/aquora-store/apps/storefront/.gcloudignore, /Users/admin/Documents/aquora/HANDOFF.md
- **Acceptance:** ✓ Secret Manager holds DATABASE_URL, COOKIE_SECRET, JWT_SECRET, STRIPE_API_KEY, STRIPE_WEBHOOK_SECRET, and the storefront NEXT_PUBLIC_* build values. ✓ aquora-backend Cloud Run references DB/Stripe/auth secrets via --set-secrets (not inline env), Cloud SQL socket preserved, and the service serves traffic after the change. ✓ The aquora-db password is rotated and the DATABASE_URL secret updated to match; the backend reconnects and the catalogue/admin still load. ✓ Deploy SA and backend runtime SA have roles/secretmanager.secretAccessor on the required secrets. ✓ No secret value appears in any command output, commit, or workflow log; the runbook records the secret inventory and rotation procedure.
- **🔒 Owner input:** Owner must approve/perform the Cloud SQL password rotation window (brief backend reconnect) and provide or authorize creation of Secret Manager secrets + the real secret values (Stripe live key, webhook secret, COOKIE/JWT secrets, new DB password). Confirm whether to rotate during a maintenance window.

#### WS0-PR7 — Write CONTRIBUTING / agent runbook documenting the build-deploy-verify loop and hard invariants  `[S/low]` · depends: WS0-PR3, WS0-PR4
- **Problem:** AO agents must execute cold with no tribal knowledge. The critical operational facts are scattered across HANDOFF.md and commit messages: the git root is the PARENT of aquora-store/; the storefront needs .env.production for NEXT_PUBLIC inlining (.gcloudignore); backend migrations run out-of-band (240s probe); the DEFERRED-SUSPENSE bug means funnel UI must render INLINE; comms are Email+WhatsApp only with a placeholder number; no fabricated data; secret files must never be printed. Without a single runbook, agents will reintroduce known-fatal regressions (checkout white-screen, hidden-Suspense funnel UI).
- **Scope:** Add CONTRIBUTING.md (and/or AGENTS.md) at the git root capturing the full agent loop: (1) Repo layout — git root is /Users/admin/Documents/aquora, app code under aquora-store/apps/{backend,storefront}, work from the aquora-store/ dir for npm/turbo. (2) Local run (from HANDOFF.md: docker compose up; backend npx medusa develop on :9000; storefront npm run dev on :8000). (3) The PR→CI→deploy flow (WS0-PR3/PR4): branch, PR, CI must pass, merge auto-deploys the changed service; SHA-tagged images; rollback via update-traffic. (4) Migrations are out-of-band (WS0-PR5) — adding a module migration requires noting it in the PR template. (5) HARD INVARIANTS as a checklist: never wrap funnel-critical UI (add-to-cart, cart count, grids) in a streamed <Suspense> (deferred-suspense bug hides it forever) — render INLINE; no fabricated reviews/stock/urgency/social-proof; Email + WhatsApp comms only (number is a +971...0000 placeholder, hasRealWhatsapp=false); never cat/print .env/.env.production/secret files; secrets live in Secret Manager. (6) Verify step — how to smoke-test a deploy (catalogue renders, PDP add-to-cart works, checkout does not white-screen). Keep it concise and actionable; link HANDOFF.md for deeper context.
- **Key files:** /Users/admin/Documents/aquora/CONTRIBUTING.md, /Users/admin/Documents/aquora/HANDOFF.md, /Users/admin/Documents/aquora/aquora-store/apps/storefront/.gcloudignore, /Users/admin/Documents/aquora/aquora-store/apps/backend/Dockerfile
- **Acceptance:** ✓ CONTRIBUTING.md (or AGENTS.md) exists at the git root and states the git-root-vs-aquora-store/ layout explicitly. ✓ It documents the full PR→CI→Cloud-Run auto-deploy loop (matching WS0-PR3/PR4) including SHA tagging and rollback. ✓ It lists the hard invariants as a checklist: no streamed Suspense around funnel UI; no fabricated data; Email+WhatsApp-only comms with the placeholder-number caveat; never print secret files; out-of-band migrations. ✓ It includes a concrete post-deploy verify checklist (catalogue, add-to-cart, no checkout white-screen). ✓ An agent reading only this file could reproduce a local run and understand how a merged PR reaches production.

#### WS0-PR8 — Configure the AO project against github.com/ralch22/aquora  `[M/med]` 🔒 · depends: WS0-PR2, WS0-PR3, WS0-PR4, WS0-PR7
- **Problem:** Once the repo is reconciled (WS0-PR1) and the PR→CI→deploy pipeline exists (WS0-PR3/PR4), the AO orchestrator (ao-agents.com) still needs to be pointed at the repo with the right defaults so it can branch, open PRs, await CI, and let merges auto-deploy. Without this configuration AO cannot actually run the later workstreams.
- **Scope:** Configure the AO project for github.com/ralch22/aquora: connect the repo, set the default base branch (main), set the agent working directory to aquora-store/ for build/test commands, register the CI required-check (from WS0-PR3) as the merge gate, and load the agent runbook (CONTRIBUTING.md from WS0-PR7) as the project's standing context so every spawned agent inherits the hard invariants. Provide AO with the build/verify commands (npm install --legacy-peer-deps; turbo lint/build; local run per HANDOFF). This is primarily AO-side setup plus any repo-side config files AO expects; no production credentials should be embedded in the repo.
- **Key files:** /Users/admin/Documents/aquora/CONTRIBUTING.md, /Users/admin/Documents/aquora/.github/workflows/ci.yml, /Users/admin/Documents/aquora/.github/pull_request_template.md
- **Acceptance:** ✓ The AO project is connected to github.com/ralch22/aquora with main as the base branch and aquora-store/ as the agent working directory. ✓ AO is configured to wait for the CI check and to rely on merge→auto-deploy (it does NOT run gcloud deploys directly). ✓ The CONTRIBUTING.md/agent runbook is loaded as standing AO context so spawned agents inherit the deferred-Suspense, no-fabrication, comms, and secret-handling invariants. ✓ A trivial end-to-end dry run (AO opens a no-op PR, CI passes, merge auto-deploys without error) succeeds.
- **🔒 Owner input:** Owner must grant AO (ao-agents.com) access to the GitHub repo ralch22/aquora (GitHub App install / token) and confirm AO project settings; owner also confirms AO should NOT hold GCP deploy credentials (deploy happens via the Actions pipeline).

---
## M1 · SEE — read the funnel before building more
*Instrument + actually read GA4. One week of looking decides where every later dollar goes.*

#### WS1-PR1 — Audit and document the GA4/GTM + Measurement Protocol env config and event map  `[S/low]` 🔒
- **Problem:** GA4 dual-mode (gtag + GTM dataLayer) wiring exists in apps/storefront/src/lib/analytics.ts and ga-script.tsx, and a server-side Measurement Protocol purchase subscriber exists in apps/backend/src/subscribers/ga-purchase.ts gated on GA4_MEASUREMENT_ID/GA4_MP_API_SECRET — but no repo file documents which env vars must be set in which service, whether they are set in the live Cloud Run revisions, or what the canonical event map is. Nobody can currently confirm GA4 is actually receiving data in production. This is the foundation for every other analytics PR.
- **Scope:** Add a docs/analytics.md (or apps/storefront and apps/backend .env.example entries) that enumerates every analytics env var (NEXT_PUBLIC_GA_ID, NEXT_PUBLIC_GTM_ID on storefront; GA4_MEASUREMENT_ID, GA4_MP_API_SECRET on backend), where each is set, and the double-count caveat already noted in ga-script.tsx. Produce a single canonical event-coverage table mapping GA4 event name → firing component/file → status (live / missing). Do NOT print or commit any secret values; reference variable names only. Add a short 'how to verify in GA4 DebugView' checklist. No runtime code changes.
- **Key files:** apps/storefront/src/lib/analytics.ts, apps/storefront/src/modules/analytics/ga-script.tsx, apps/backend/src/subscribers/ga-purchase.ts, apps/storefront/src/modules/analytics/begin-checkout-tracker.tsx
- **Acceptance:** ✓ A committed markdown doc lists all four analytics env vars, their owning service, and purpose — with no secret values printed ✓ Doc contains an event-coverage table with one row per GA4 ecommerce event and a live/missing status grounded in the actual repo ✓ Doc includes a DebugView verification checklist an owner can follow ✓ No production runtime code is modified by this PR
- **🔒 Owner input:** Confirm which env vars are actually set in the live storefront (rev 00071) and backend (v22) Cloud Run revisions, and whether a real GA4 property + MP API secret exist; provide the GA4 property/stream id (not the secret) so the doc can name it.

#### WS1-PR2 — Add view_item_list + select_item GA4 events to product grids and rails  `[M/med]` · depends: WS1-PR1
- **Problem:** Product discovery is invisible to GA4: ProductPreview (apps/storefront/src/modules/products/components/product-preview/index.tsx) is a server component that fires no events, so view_item_list (impressions) and select_item (clicks into a PDP) are completely missing. Without these we cannot measure top-of-funnel: how many product impressions convert to PDP views, or which lists/rails/categories drive clicks.
- **Scope:** Add a small client wrapper (e.g. a 'use client' tracker mounted by ProductPreview or its parent grid/rail) that fires GA4 view_item_list on impression and select_item on click, passing item_list_name/item_list_id (e.g. 'search', 'category:<handle>', 'related', 'featured'). Reuse the existing toItem/emit helpers in analytics.ts by adding trackViewItemList and trackSelectItem functions there. Must render INLINE — never inside a streamed <Suspense> (deferred-suspense bug). Use IntersectionObserver for impressions, throttled to avoid event spam. No fabricated data — only real product id/name/price already on the product object.
- **Key files:** apps/storefront/src/lib/analytics.ts, apps/storefront/src/modules/products/components/product-preview/index.tsx, apps/storefront/src/modules/home/components/featured-products/product-rail, apps/storefront/src/modules/products/components/related-products, apps/storefront/src/app/[countryCode]/(main)/search/page.tsx
- **Acceptance:** ✓ analytics.ts exports trackViewItemList and trackSelectItem using the existing emit/toItem pattern with currency AED and an items array ✓ view_item_list fires once when a grid/rail enters the viewport with the correct item_list_name ✓ select_item fires on click-through to a PDP carrying the same item_list_name and the clicked item ✓ Tracking renders inline (no streamed Suspense wrapping product grids) ✓ Events visible in GA4 DebugView with real product ids/names from the catalog

#### WS1-PR3 — Add GA4 search, view_cart, and remove_from_cart events  `[M/med]` · depends: WS1-PR1
- **Problem:** Three standard funnel/engagement events are absent from GA4. Search currently only emits a Google Retail event (apps/storefront/src/modules/analytics/search-tracker.tsx → trackRetailEvent('search')), so GA4 has no 'search' event and we cannot see search usage or zero-result queries. The cart page fires no view_cart, and quantity removal/decrement fires no remove_from_cart — so mid-funnel cart abandonment is unmeasurable.
- **Scope:** Add trackSearch, trackViewCart, trackRemoveFromCart to analytics.ts (reusing emit/toItem). Fire GA4 'search' (with search_term) alongside the existing Retail event in SearchTracker. Fire view_cart when the cart page/drawer renders with its line items and value. Fire remove_from_cart when a line is removed or quantity decremented (find the existing optimistic cart qty / remove handler). All inline, AED currency, real values only. Do not double-fire on the existing Retail search path.
- **Key files:** apps/storefront/src/lib/analytics.ts, apps/storefront/src/modules/analytics/search-tracker.tsx, apps/storefront/src/lib/aquora/retail-track.ts, apps/storefront/src/modules/products/components/product-actions/index.tsx
- **Acceptance:** ✓ GA4 'search' event fires with search_term equal to the user query, in addition to the existing Retail search event (no regression to Retail) ✓ view_cart fires once when the cart view renders, with currency AED, value and items ✓ remove_from_cart fires with the correct item and quantity when a line is removed or decremented ✓ All three render inline with no streamed Suspense; events confirmed in GA4 DebugView

#### WS1-PR4 — Harden the server-side Measurement Protocol purchase event (dedupe + debug validation)  `[M/med]` 🔒 · depends: WS1-PR1
- **Problem:** The MP purchase subscriber (apps/backend/src/subscribers/ga-purchase.ts) fires on order.placed but the client also fires a GA4 purchase via PurchaseTracker — risking double-counted revenue in GA4 if both reach the same property without a shared dedupe key. It also has no validation path and silently swallows all errors, so a misconfigured api_secret produces zero feedback. Reliable purchase data is the single most important conversion metric.
- **Scope:** Add a stable shared transaction_id/dedupe between client (PurchaseTracker, apps/storefront/src/modules/analytics/purchase-tracker.tsx) and server MP events so GA4 de-duplicates (use the order id as transaction_id consistently and document the dedupe strategy). In the subscriber, add a debug/validation mode (POST to the GA4 /debug/mp/collect endpoint when a DEBUG flag is set) and structured logging of non-2xx responses so misconfiguration is visible. Keep best-effort non-blocking on the order flow. No secrets in logs.
- **Key files:** apps/backend/src/subscribers/ga-purchase.ts, apps/storefront/src/modules/analytics/purchase-tracker.tsx, apps/storefront/src/modules/analytics/begin-checkout-tracker.tsx, apps/storefront/src/lib/analytics.ts
- **Acceptance:** ✓ Client and server purchase events share the same transaction_id (order id) so GA4 de-duplicates rather than double-counting revenue ✓ Subscriber logs a structured warning (status only, no secret) when MP returns a non-2xx response ✓ A DEBUG-flagged path hits the GA4 debug MP endpoint and surfaces validationMessages ✓ Order placement still succeeds even when MP env vars are unset or the call fails
- **🔒 Owner input:** Confirm whether GA4_MEASUREMENT_ID/GA4_MP_API_SECRET are set on the backend and whether the GA4 property currently receives the client-side purchase (so we know which side is canonical for the dedupe decision).

#### WS1-PR5 — Build a lightweight, deferred-suspense-safe A/B test harness  `[M/med]` · depends: WS1-PR1, WS1-PR2
- **Problem:** Future conversion PRs (CTA copy, layout, badges) are currently guesses with no way to measure lift. There is no experiment/variant assignment primitive in the storefront (only an unrelated dosing-calculator matched 'variant'). We need a minimal, dependency-free harness that buckets visitors deterministically and reports the assignment to GA4 so any conversion change can be A/B-measured.
- **Scope:** Add a small experiments module: a deterministic bucketing function (hash of the existing aq_vid visitor id from retail-track.ts + experiment key → variant) persisted in a cookie/localStorage, a React hook/provider to read the active variant, and an automatic GA4 event (e.g. 'experiment_impression' with experiment_id + variant_id, or a user_property) so GA4 can segment conversions by variant. Provide one trivial wired example (a single CTA or copy string) to prove the loop end-to-end. Variants must render INLINE (no streamed Suspense) to avoid the deferred-suspense bug, and assignment must be SSR-stable to avoid hydration flicker.
- **Key files:** apps/storefront/src/lib/analytics.ts, apps/storefront/src/lib/aquora/retail-track.ts, apps/storefront/src/modules/analytics/ga-script.tsx
- **Acceptance:** ✓ A getVariant(experimentKey, variants) util deterministically and stably assigns the same visitor to the same variant across reloads ✓ Variant assignment is sent to GA4 (event or user_property) so conversions can be segmented by variant_id in GA4 ✓ One real example experiment is wired and renders inline with no hydration mismatch and no streamed Suspense ✓ Disabling/removing an experiment leaves the control rendering with zero residual events

#### WS1-PR6 — Ship a funnel/conversion report definition to answer 'where do they drop'  `[M/low]` 🔒 · depends: WS1-PR1, WS1-PR2, WS1-PR3
- **Problem:** Even with events flowing, there is no artifact that answers the core business question: is there traffic, and where does the funnel leak (view_item_list→view_item→add_to_cart→begin_checkout→add_payment_info→purchase)? The owner has no dashboard or repeatable report, so the 'next dollar' decision is blind.
- **Scope:** Deliver a committed, reproducible funnel report definition rather than a one-off screenshot: a docs/analytics-funnel.md describing the exact GA4 Exploration funnel steps (matching the event names this workstream standardizes) and the comparison dimensions (device, item_list_name, source/medium), PLUS a runnable script (apps/backend/scripts or a Node script) that queries the GA4 Data API for the funnel step counts and prints stage-to-stage conversion rates. The script must be credential-gated and no-op with a clear message when creds are absent. No fabricated numbers; output only what the API returns.
- **Key files:** apps/backend/src/subscribers/ga-purchase.ts, apps/storefront/src/lib/analytics.ts, apps/backend/package.json
- **Acceptance:** ✓ docs/analytics-funnel.md specifies the GA4 Exploration funnel steps using the canonical event names from this workstream ✓ A runnable script queries the GA4 Data API and prints per-stage counts and stage-to-stage conversion rates ✓ Script exits gracefully with an instructive message when GA4 Data API credentials/property id are not configured ✓ No numbers are hardcoded or fabricated; all output derives from the live API response
- **🔒 Owner input:** Provide (or grant a service account access to) the GA4 property id and enable the GA4 Data API so the report script can authenticate; confirm the property is the same one the storefront tags write to.

---
## M2 · SELLABLE — let the cart hold a real order
*Fix inventory-capped-at-1, promotions, bundles, BNPL, facets.*

#### WS4-PR1 — Diagnose and document the inventory-capped-at-1 root cause across import + storefront  `[S/low]`
- **Problem:** Most products show inventory_quantity=1 in production, so the PDP quantity stepper maxes at 1 and customers can't buy multiples of high-ticket gear. The import script (apps/backend/src/scripts/import-catalog.ts) actually writes stocked_quantity: 100 with manage_inventory: true, yet production reads 1 — so production diverged from the script (a likely earlier import default or a partially-run inventory step). Before any bulk write we must know exactly which variants are affected and why, so the fix is targeted and safe.
- **Scope:** Write a read-only diagnostic script under apps/backend/src/scripts (e.g. audit-inventory.ts) that, against the live Cloud SQL DB, reports: count of variants by manage_inventory true/false, distribution of inventory_quantity / stocked_quantity values (how many at exactly 1 vs 100 vs other), how many variants have inventory levels at the Dubai Distribution Centre stock location, and how many have allow_backorder set. Output a CSV/summary to stdout (no DB writes). Cross-reference the storefront qty-cap logic in apps/storefront/src/modules/products/components/product-actions/index.tsx (lines ~110-140) and listProducts fields in apps/storefront/src/lib/data/products.ts to confirm which field actually gates the stepper. Produce a short written findings note in the PR description recommending the fix path (manage_inventory=false for made-to-order vs real stock levels).
- **Key files:** apps/backend/src/scripts/import-catalog.ts, apps/backend/src/lib/product-lookup.ts, apps/storefront/src/modules/products/components/product-actions/index.tsx, apps/storefront/src/lib/data/products.ts
- **Acceptance:** ✓ Running the script prints a breakdown of variant inventory state (manage_inventory true/false counts, inventory_quantity value histogram, count of variants with vs without a stock level at the Dubai location, allow_backorder counts). ✓ The script performs zero writes (read/query only) and is safe to run against production. ✓ PR description states the confirmed root cause of the cap-at-1 and a recommended remediation path with the exact field to change. ✓ No secrets are printed; DB connection uses existing env/config, not hardcoded credentials.

#### WS4-PR2 — Safe bulk inventory remediation: backorder/made-to-order stock model  `[M/high]` 🔒 · depends: WS4-PR1
- **Problem:** Once the cap-at-1 cause is known (WS4-PR1), the catalog needs a corrective bulk update so customers can buy quantities >1. Because Aquora is largely distributor/made-to-order equipment, the safest honest model is to either set real stock levels or set variants to not block on inventory — without fabricating specific stock counts.
- **Scope:** Add an idempotent, re-runnable bulk-update script under apps/backend/src/scripts (e.g. fix-inventory.ts) controlled by an explicit flag/env, implementing the remediation chosen by the owner: EITHER set affected variants to manage_inventory=false (made-to-order, qty unrestricted, honest 'available to order' messaging) OR upsert real stocked_quantity inventory levels at the Dubai Distribution Centre stock location for affected inventory items. Batch writes (e.g. 1000 at a time, mirroring the existing inventory loop at import-catalog.ts ~line 200-203), log progress, and be safe to re-run. Verify against the storefront: PDP stepper must allow qty>1 and honest stock/availability messaging must remain truthful (no invented stock numbers). Do NOT change the deferred-Suspense-sensitive add-to-cart rendering.
- **Key files:** apps/backend/src/scripts/import-catalog.ts, apps/backend/src/scripts/fix-inventory.ts, apps/storefront/src/modules/products/components/product-actions/index.tsx
- **Acceptance:** ✓ Running the script updates affected variants per the owner-chosen model and is idempotent (a second run makes no further changes / errors out cleanly). ✓ After running, a sample of previously-capped PDPs allows selecting quantity >1 and adding multiples to cart. ✓ Stock/availability copy remains honest — no fabricated specific stock numbers shown to shoppers. ✓ Script batches writes and logs counts; a dry-run mode prints what would change without writing.
- **🔒 Owner input:** Decide the stock model: (a) treat the catalog as made-to-order → set manage_inventory=false for affected variants, OR (b) real stock → provide actual stocked_quantity levels (or a default like 100) to write. Confirm which variants are genuinely limited stock vs unlimited.

#### WS4-PR3 — Seed a real Medusa promotions engine so the live discount-code box accepts codes  `[M/med]` 🔒
- **Problem:** The storefront already renders an 'Add Promotion Code(s)' box (apps/storefront/src/modules/checkout/components/discount-code/index.tsx) wired to applyPromotions() in apps/storefront/src/lib/data/cart.ts, but production has no actual promotions defined, so every code returns an error and the box looks broken. The merchandising/discount lever is dead until real promotions exist.
- **Scope:** Add a backend script under apps/backend/src/scripts (e.g. seed-promotions.ts) that creates a small set of real Medusa promotions via the promotion module/workflow: at minimum a launch code (e.g. percentage or fixed-amount off order) and the scaffolding for automatic promotions. Keep amounts/codes parameterized so the owner can set real values. Ensure currency is aed and the single UAE region is targeted. Verify end-to-end: applying a valid code in the live discount box reduces the cart total and shows the applied badge; an invalid code shows the existing error message. Do not touch the discount-code component's inline rendering (must not introduce a streamed Suspense around cart totals).
- **Key files:** apps/backend/src/scripts/seed-promotions.ts, apps/storefront/src/modules/checkout/components/discount-code/index.tsx, apps/storefront/src/lib/data/cart.ts, apps/backend/medusa-config.ts
- **Acceptance:** ✓ After running the script, at least one real promotion code exists and applying it in the checkout discount box reduces the cart total and shows the applied-promotion badge. ✓ An invalid/expired code still surfaces the existing inline error message without crashing. ✓ Promotion amounts/codes are parameterized (env or script args), not hardcoded marketing values. ✓ Currency aed and the UAE region are correctly targeted; no fabricated urgency/scarcity copy is added.
- **🔒 Owner input:** Approve the actual launch promo: code string(s), discount type/amount, any minimum-order threshold, and expiry. Confirm it's a real offer the business will honor.

#### WS4-PR4 — Bundle/kit merchandising ('buy the set, save') via Medusa promotions  `[L/med]` 🔒 · depends: WS4-PR3
- **Problem:** High-ticket pool/spa builds are naturally bought as sets (pump + filter + valve, dosing kit, etc.), but there's no bundle concept or 'buy the set, save' merchandising anywhere in the storefront or backend. A buy-the-set offer is a proven AOV lever for equipment.
- **Scope:** Implement curated bundles as buy-X-get-discount Medusa promotions (no new product type required): a backend script under apps/backend/src/scripts (e.g. seed-bundles.ts) defining 2-3 starter kits as automatic promotions that trigger a saving when the bundle's component products/variants are all in the cart. Add a small storefront 'Frequently bought together / Complete the set' module that lists the bundle's components with an add-all-to-cart action, rendered INLINE (no streamed Suspense — funnel-critical). Place it on the PDP (reuse patterns from apps/storefront/src/modules/products/components/product-actions and product-lookup hydration in apps/backend/src/lib/product-lookup.ts). Bundle membership defined via product metadata or a static curated map under apps/storefront/src/lib/aquora.
- **Key files:** apps/backend/src/scripts/seed-bundles.ts, apps/backend/src/lib/product-lookup.ts, apps/storefront/src/modules/products/components/product-actions/index.tsx, apps/storefront/src/lib/aquora/categories.ts, apps/storefront/src/lib/data/cart.ts
- **Acceptance:** ✓ At least 2 curated bundles exist; adding all components of a bundle to the cart automatically applies the bundle saving and shows it in the cart totals. ✓ The PDP 'Complete the set' module renders inline (visible without interaction; no streamed Suspense) and lists real component products with prices. ✓ Add-all-to-cart adds every component variant to the cart in one action. ✓ Only real products and real savings are shown — no fabricated 'others also bought' social proof.
- **🔒 Owner input:** Confirm which products form each bundle and the bundle discount amount/percentage for each kit.

#### WS4-PR5 — Integrate UAE BNPL (Tabby/Tamara) as a checkout payment provider  `[L/high]` 🔒
- **Problem:** Aquora sells high-ticket pool/spa equipment in the UAE/GCC where Tabby and Tamara 'split in installments' are a major conversion lever. Today medusa-config.ts only registers @medusajs/payment-stripe (id 'stripe'); there is no BNPL option, and no 'pay in installments' messaging on PDPs.
- **Scope:** Register a Tabby and/or Tamara payment provider in apps/backend/medusa-config.ts (via their Medusa provider plugin or a thin custom payment provider module under apps/backend/src/modules) so it appears as a selectable method in the existing checkout payment step. Wire credentials via env/secrets (never hardcoded). Add honest 'Pay in 4 with Tabby/Tamara' messaging on the PDP/checkout only when the provider is configured (feature-flag off if creds absent, similar to the hasRealWhatsapp pattern). Keep express-checkout isolation intact and do not regress the inline add-to-cart/checkout rendering.
- **Key files:** apps/backend/medusa-config.ts, apps/backend/src/api/store, apps/storefront/src/modules/checkout/components/discount-code/index.tsx, apps/storefront/src/modules/products/components/product-actions/index.tsx
- **Acceptance:** ✓ With valid credentials set, Tabby and/or Tamara appears as a payment method in the checkout payment step and a test order can be initiated through it. ✓ When credentials are absent, the BNPL option and 'pay in installments' messaging are hidden (no broken/dark CTA), mirroring the hasRealWhatsapp gating. ✓ No secrets are committed or printed; all BNPL keys come from env/secret store. ✓ Stripe card + Apple/Google Pay checkout continues to work unchanged.
- **🔒 Owner input:** Provide Tabby and/or Tamara UAE merchant accounts and API credentials (public/secret keys, merchant code), and confirm which provider(s) to enable.

#### WS4-PR6 — PLP facets & filtering quality at 6k-product scale (category + brand + price)  `[M/med]`
- **Problem:** With ~5,935 products, the store listing is hard to navigate. The current refinement-list (apps/storefront/src/modules/store/components/refinement-list) only offers a global product-options picker and sort; there's no brand facet or price-range filter even though brand lives in product metadata and categories are well-defined (apps/storefront/src/lib/aquora/categories.ts). Shoppers can't narrow 6k SKUs to what they need.
- **Scope:** Add brand and price-range facets to the PLP. Source brand values from product metadata.brand (see import-catalog/extract-brands and product-lookup mapping of metadata.brand) and category from the existing categories lib. Implement the filters as URL-param-driven refinements (mirror the existing updateQueryParams pattern in refinement-list/index.tsx) and apply them to the product query in apps/storefront/src/lib/data/products.ts (listProducts queryParams). Ensure the product grid and facet controls render INLINE (no streamed Suspense around the grid — funnel-critical). Keep it performant: facet option lists should be bounded (e.g. top brands per category) rather than enumerating all 6k.
- **Key files:** apps/storefront/src/modules/store/components/refinement-list/index.tsx, apps/storefront/src/modules/store/components/refinement-list/options-picker/index.tsx, apps/storefront/src/modules/store/templates/paginated-products.tsx, apps/storefront/src/lib/data/products.ts, apps/storefront/src/lib/aquora/categories.ts, apps/backend/src/scripts/extract-brands.ts
- **Acceptance:** ✓ PLP shows a brand facet (populated from real metadata.brand values) and a price-range filter, both driven by URL params so they're shareable and survive refresh. ✓ Selecting a brand and/or price range narrows the product grid to matching products and updates pagination correctly. ✓ The product grid and facets render inline (visible without interaction; no streamed Suspense). ✓ Facet option lists are bounded and the page remains responsive at 6k-product scale.

---
## M3 · BELIEVABLE — fill the empty trust containers
*Real WhatsApp, real reviews engine + ratings, GBP. Never fabricated.*

#### WS3-PR1 — Set the real WhatsApp number and verify all gated CTAs light up  `[S/low]` 🔒
- **Problem:** contact.whatsapp in apps/storefront/src/lib/aquora/brand.ts is the placeholder "+971 50 000 0000", so hasRealWhatsapp=false and every built-in WhatsApp CTA (footer, PDP trust-strip, contact page) is dark. The site has no working WhatsApp channel even though the code is ready to switch on the moment a real number (no run of 4+ zeros) is set.
- **Scope:** Replace the placeholder whatsapp value (and phone if the owner provides one) in the `contact` object in apps/storefront/src/lib/aquora/brand.ts with the owner-supplied real number in international format. Do NOT touch the hasRealWhatsapp/hasRealPhone gating logic. After the change, manually confirm the three existing CTAs now render and produce correct wa.me links: footer (apps/storefront/src/modules/layout/templates/footer/index.tsx), PDP trust-strip (apps/storefront/src/modules/products/components/trust-strip/index.tsx), and contact page (apps/storefront/src/app/[countryCode]/(main)/contact/page.tsx).
- **Key files:** apps/storefront/src/lib/aquora/brand.ts, apps/storefront/src/modules/layout/templates/footer/index.tsx, apps/storefront/src/modules/products/components/trust-strip/index.tsx, apps/storefront/src/app/[countryCode]/(main)/contact/page.tsx
- **Acceptance:** ✓ contact.whatsapp in brand.ts is a real number with no run of 4+ zeros, so hasRealWhatsapp evaluates true ✓ Footer renders a WhatsApp link with href https://wa.me/<digits> (no spaces/symbols) ✓ PDP trust-strip renders the 'Enquire on WhatsApp' button with a wa.me link that pre-fills the product title message ✓ Contact page renders the WhatsApp contact row ✓ No placeholder all-zeros number remains anywhere in brand.ts; build passes
- **🔒 Owner input:** The real Aquora WhatsApp business number in international format (e.g. +971 5X XXX XXXX), and optionally the real landline phone if it should also go live.

#### WS3-PR2 — Add a floating WhatsApp enquire button (gated, inline-rendered)  `[S/low]` · depends: WS3-PR1
- **Problem:** Once a real WhatsApp number exists, the only CTAs are in the footer, PDP trust-strip and contact page — there is no persistent, always-visible WhatsApp entry point for quick pre-sales enquiries, which is the primary expected contact channel for UAE pool buyers. Comms policy is Email + WhatsApp only.
- **Scope:** Create a small client component (e.g. apps/storefront/src/modules/layout/components/whatsapp-fab/index.tsx) that renders a fixed-position floating WhatsApp button linking to https://wa.me/<digits> with a generic pre-filled message. It MUST render only when hasRealWhatsapp is true (import from @lib/aquora/brand) and MUST render INLINE — never inside a streamed <Suspense> (per the deferred-suspense bug). Mount it in the main layout (apps/storefront/src/app/[countryCode]/(main)/layout.tsx). Avoid colliding with the existing ai-assistant launcher (apps/storefront/src/modules/layout/components/ai-assistant/index.tsx) and compare-bar — offset z-index/position so both are tappable on mobile.
- **Key files:** apps/storefront/src/modules/layout/components/ai-assistant/index.tsx, apps/storefront/src/modules/layout/components/compare-bar/index.tsx, apps/storefront/src/app/[countryCode]/(main)/layout.tsx, apps/storefront/src/lib/aquora/brand.ts
- **Acceptance:** ✓ A floating WhatsApp button appears on all main-layout pages only when hasRealWhatsapp is true (renders nothing when the number is the placeholder) ✓ Button links to https://wa.me/<digits>?text=<encoded message> and opens in a new tab with rel=noopener ✓ Button does not overlap or block the Ask-Aqua assistant launcher or the compare bar on mobile (375px) or desktop ✓ Component renders inline (no streamed Suspense wrapper); add-to-cart and other funnel UI unaffected

#### WS3-PR3 — Expose real review aggregateRating in Product JSON-LD  `[M/med]`
- **Problem:** Reviews are stored and displayed on the PDP, but apps/storefront/src/modules/products/components/product-jsonld/index.tsx emits a Product schema with NO aggregateRating, so Google can never show review stars in search results. The data exists (GET /store/reviews returns {aggregate:{count,average}}) but is client-fetched and never reaches the server-rendered schema.
- **Scope:** On the server, fetch the review aggregate for the product (count + average) from the backend GET /store/reviews?product_id=... endpoint (add a small server data helper in apps/storefront/src/lib/data/products.ts or a new reviews.ts), and pass it into ProductJsonLd. When count > 0, add an aggregateRating block (@type AggregateRating, ratingValue=average, reviewCount=count, bestRating 5, worstRating 1) to the Product schema. When count is 0, emit NO aggregateRating (never fabricate a rating). Keep the fetch resilient (fail to no-rating on error) and cached/non-blocking so PDP render is unaffected.
- **Key files:** apps/storefront/src/modules/products/components/product-jsonld/index.tsx, apps/storefront/src/modules/products/templates/index.tsx, apps/storefront/src/lib/data/products.ts, apps/backend/src/api/store/reviews/route.ts
- **Acceptance:** ✓ For a product with >=1 approved review, the Product JSON-LD includes aggregateRating with ratingValue equal to the API average and reviewCount equal to the API count ✓ For a product with 0 approved reviews, NO aggregateRating field is present in the JSON-LD ✓ Google Rich Results Test validates the Product markup with review snippet eligible when reviews exist ✓ PDP still renders if the reviews backend call fails (schema simply omits aggregateRating)

#### WS3-PR4 — Show real star ratings on product cards and grids  `[M/med]` · depends: WS3-PR3
- **Problem:** Ratings only appear on the PDP. Product cards (apps/storefront/src/modules/products/components/product-preview/index.tsx) used across the store grid, category pages, related/recommended rails and home shelves show no rating, so the social proof from real reviews never reaches browse/listing surfaces where it most aids click-through.
- **Scope:** Add a compact star + count display to the product card. To avoid per-card N+1 calls, add a backend batch aggregate endpoint (e.g. GET /store/reviews/aggregate?product_ids=a,b,c returning {[id]:{count,average}}) in apps/backend/src/api/store/reviews/, fetch aggregates once per grid/rail on the server, and pass an optional rating prop down to ProductPreview. Render stars only when count > 0 (never show '0 reviews' as a rating or any fabricated value); cards with no reviews render exactly as today. Keep the card rendering inline (no streamed Suspense).
- **Key files:** apps/storefront/src/modules/products/components/product-preview/index.tsx, apps/storefront/src/modules/products/components/reviews/index.tsx, apps/backend/src/api/store/reviews/route.ts, apps/storefront/src/lib/data/products.ts
- **Acceptance:** ✓ A new GET /store/reviews/aggregate accepts a comma-separated product_ids list and returns count+average per id for approved reviews only ✓ Product cards with >=1 approved review show a small star rating and review count ✓ Product cards with 0 reviews show no rating element (no zeros, no placeholder stars) ✓ Store grid / category / rails make at most one aggregate request per page render (no per-card fetch) ✓ Card grids render inline; add-to-cart and grid layout unaffected

#### WS3-PR5 — Add photo (UGC) support to reviews end-to-end  `[L/med]`
- **Problem:** The review model (apps/backend/src/modules/review/models/review.ts) has no image field, so customers can't attach photos of their installed equipment — the highest-trust form of social proof. The submission form, store API, moderation page and PDP display all lack photo handling.
- **Scope:** Add an images field (array of URLs, capped e.g. 4) to the Review model with a migration, accept and validate image URLs in POST /store/reviews, return them from GET /store/reviews, render an upload control in the storefront review form (apps/storefront/src/modules/products/components/reviews/index.tsx) and a photo thumbnail strip/lightbox in the review list, and show submitted photos in the admin moderation page (apps/backend/src/admin/routes/reviews/page.tsx) so the owner can reject inappropriate images. Use the existing upload/file mechanism the backend already exposes for product images; do NOT invent a new storage bucket. Photos only ever come from genuine submissions (no stock/fabricated imagery).
- **Key files:** apps/backend/src/modules/review/models/review.ts, apps/backend/src/modules/review/migrations/Migration20260628084606.ts, apps/backend/src/api/store/reviews/route.ts, apps/storefront/src/modules/products/components/reviews/index.tsx, apps/backend/src/admin/routes/reviews/page.tsx
- **Acceptance:** ✓ Review model has an images field and a generated migration that applies cleanly ✓ POST /store/reviews accepts up to N image URLs, validates them, and persists them; GET /store/reviews returns images for approved reviews ✓ Storefront review form lets a user attach 1-4 photos and they appear on the approved review card (with a click-to-enlarge lightbox) ✓ Admin moderation page shows submitted photos so the owner can approve/reject; rejected reviews never expose images publicly ✓ Reviews without photos render exactly as before

#### WS3-PR6 — Import and display real Google Business Profile reviews (store-wide social proof)  `[L/med]` 🔒
- **Problem:** Aquora's product-level reviews start empty; meanwhile any real Google Business Profile rating/reviews are an existing, credible trust signal that is not surfaced anywhere on the site. There is no store-wide reviews/ratings display, only per-product. We must show ONLY the real GBP rating — never a fabricated one.
- **Scope:** Build a backend integration that fetches the business's real Google rating + recent reviews via the Google Places/Business Profile API (using an owner-provided place_id + API key as env vars), caches them server-side, and exposes them via a store endpoint (e.g. GET /store/google-reviews). Add a storefront 'What customers say' section (e.g. on home and/or about page) that renders the real Google star rating, total count, and a few real review snippets with clear 'via Google' attribution and a link to the live Google listing. Render inline (no streamed Suspense). Gate the whole section so it renders nothing until the place_id/API key are configured (no placeholder ratings). Respect Google's review display/attribution terms.
- **Key files:** apps/backend/src/lib/retail.ts, apps/backend/src/api/store/reviews/route.ts, apps/storefront/src/modules/home/components/brand-story/index.tsx, apps/storefront/src/app/[countryCode]/(main)/page.tsx, apps/storefront/src/lib/aquora/brand.ts
- **Acceptance:** ✓ A backend endpoint returns the real GBP rating, review count, and a capped list of real review snippets, cached and resilient to API failure ✓ A storefront section displays the real Google rating with 'via Google' attribution and a link to the live listing when the integration is configured ✓ When place_id/API key are not set, the section renders nothing (no fabricated rating, no placeholder) ✓ No Google review text/rating is altered or invented; attribution and listing link are present ✓ Section renders inline and does not regress funnel UI
- **🔒 Owner input:** The Google Business Profile / Places place_id for Aquora and a Google Places API key (set as backend env vars by the owner), plus confirmation that the GBP listing is the canonical one to display.

---
## M4 · FOUND — put traffic into the now-good funnel
*PSEO, schema, CWV, email lifecycle, product feeds.*

#### WS6-PR1 — Add robots.txt + canonical host normalization for crawlability  `[S/low]`
- **Problem:** There is no robots.txt anywhere in the storefront (apps/storefront/public has only favicon.ico + images). Search engines have no sitemap pointer and faceted/duplicate URLs (sortBy, page, optionValueIds query params on category/store pages) are freely crawlable, wasting crawl budget at 6k-product scale. The sitemap exists at apps/storefront/src/app/sitemap.ts but nothing advertises it.
- **Scope:** Add apps/storefront/src/app/robots.ts (Next MetadataRoute.Robots) that allows crawling, disallows query-param-faceted paths (e.g. /*?sortBy=, /*?optionValueIds=, /account, /checkout, /cart, /wishlist, /compare, /order), and points sitemap at `${NEXT_PUBLIC_BASE_URL}/sitemap.xml`. Use the same BASE env resolution pattern already used in sitemap.ts. Do not add static robots.txt in public/ (would conflict).
- **Key files:** apps/storefront/src/app/sitemap.ts, apps/storefront/src/lib/util/env.ts, apps/storefront/public/
- **Acceptance:** ✓ GET /robots.txt returns 200 with a Sitemap: line pointing to the live sitemap URL ✓ Disallow rules cover /checkout, /cart, /account, /order, and the faceted query params used by category/store pages ✓ robots.ts uses the same BASE/getBaseURL env resolution as sitemap.ts (no hardcoded prod host) ✓ Build succeeds and no static public/robots.txt is added

#### WS6-PR2 — Turn off unoptimized images and enable AVIF/WebP for LCP/CWV  `[M/med]`
- **Problem:** apps/storefront/next.config.js sets images.unoptimized:true, so EVERY product/banner image is served at full original size with no responsive srcset or modern format. At 6k products and image-heavy PDP/grids this is the single biggest Core Web Vitals (LCP) liability. The site runs on Cloud Run behind a Cloudflare Worker, so optimization can be done by Next's default loader or delegated to Cloudflare.
- **Scope:** In apps/storefront/next.config.js, remove unoptimized:true and configure images.formats:['image/avif','image/webp'] with sensible deviceSizes/imageSizes; confirm remotePatterns already cover storage.googleapis.com (they do). Audit that the LCP hero/product images use next/image with priority and explicit width/height (no layout shift). Do NOT wrap funnel-critical grids/images in a streamed Suspense (deferred-suspense bug). If Next's on-the-fly optimization is not viable on Cloud Run, document the Cloudflare-image alternative in the PR but still ship format negotiation.
- **Key files:** apps/storefront/next.config.js, apps/storefront/src/modules/products/components, apps/storefront/src/modules/home
- **Acceptance:** ✓ images.unoptimized:true is removed from next.config.js ✓ Product and banner images are served as AVIF/WebP with a responsive srcset (verify via DevTools Network on a PDP and a category grid) ✓ LCP image carries priority and explicit dimensions; no new CLS introduced ✓ No funnel-critical grid/image is moved into a streamed <Suspense> ✓ Local prod build (next build) succeeds and pages render images inline

#### WS6-PR3 — Programmatic brand landing pages (/brands/[brand]) with metadata + JSON-LD  `[L/med]`
- **Problem:** apps/storefront/src/lib/aquora/brands.ts lists 40+ real brands (AstralPool 1519, Atecpool 901, Pentair 309, etc.) but the brands page (apps/storefront/src/app/[countryCode]/(main)/brands/page.tsx) links each brand to /search?q=BrandName — a non-indexable, thin search result. There are zero real per-brand pages, so we capture no '[brand] pool pump UAE' demand at scale.
- **Scope:** Create apps/storefront/src/app/[countryCode]/(main)/brands/[brand]/page.tsx: a real landing page per brand slug, listing that brand's products (query backend by product metadata.brand, reusing the store/category product-list + pagination patterns), with generateStaticParams from brands.ts, generateMetadata (title/description/canonical), CollectionPage/BreadcrumbList JSON-LD (follow the pattern in product-jsonld), and inline-rendered grid (no streamed Suspense). Update brands/page.tsx tiles to link to /brands/[slug]. Add brand URLs to sitemap.ts. No fabricated copy beyond real brand counts/descriptions.
- **Key files:** apps/storefront/src/lib/aquora/brands.ts, apps/storefront/src/app/[countryCode]/(main)/brands/page.tsx, apps/storefront/src/app/[countryCode]/(main)/categories/[...category]/page.tsx, apps/storefront/src/modules/store/components/refinement-list/sort-products.tsx, apps/storefront/src/app/sitemap.ts, apps/storefront/src/modules/products/components/product-jsonld/index.tsx
- **Acceptance:** ✓ /ae/brands/astralpool (and other brand slugs) renders a paginated product grid filtered to that brand's products, inline (no streamed Suspense) ✓ Each brand page emits unique title/description + canonical and CollectionPage/BreadcrumbList JSON-LD that validates ✓ brands/page.tsx tiles link to /brands/[slug] instead of /search?q= ✓ Brand page URLs appear in sitemap.ts output ✓ Unknown brand slug returns notFound()

#### WS6-PR4 — Add hreflang/x-default alternates + per-page canonical across templates  `[M/low]` · depends: WS6-PR3
- **Problem:** The store is single-region (UAE) with en+ar audiences (site-jsonld already declares availableLanguage en/ar) but no page emits hreflang alternates, and only category pages set a canonical. Root layout (apps/storefront/src/app/layout.tsx) sets only metadataBase. Missing alternates risk duplicate-content and country-targeting issues for Gulf search.
- **Scope:** Add a shared metadata helper (e.g. apps/storefront/src/lib/util/seo.ts) that builds alternates.canonical + alternates.languages with an x-default and the en-AE/ar-AE (or current) hreflang entries keyed off [countryCode]. Wire it into generateMetadata for products, categories, collections, brands, and key static pages. Keep it data-driven from the existing region/country list (lib/data/regions). Do not change visible UI.
- **Key files:** apps/storefront/src/app/layout.tsx, apps/storefront/src/app/[countryCode]/(main)/categories/[...category]/page.tsx, apps/storefront/src/app/[countryCode]/(main)/products/[handle]/page.tsx, apps/storefront/src/lib/util/env.ts, apps/storefront/src/lib/data/regions.ts
- **Acceptance:** ✓ Product, category, collection and brand pages emit a self-referencing canonical and hreflang alternates including x-default ✓ Helper derives locales from the configured region(s), not hardcoded ✓ Rendered <head> shows rel=alternate hreflang links on a sampled PDP and category page ✓ No duplicate or conflicting canonical tags; build succeeds

#### WS6-PR5 — Newsletter signup capture + Resend welcome email (double opt-in friendly)  `[M/med]` 🔒
- **Problem:** There is no email-capture surface anywhere in the storefront (grep for newsletter/subscribe returns nothing) and no welcome email — the email lifecycle only fires on orders (order-confirmation, order-shipped, review-request jobs already exist). Without top-of-funnel capture there is no audience for browse/cart/winback flows and no list to grow paid retargeting from.
- **Scope:** Storefront: add a footer/inline newsletter signup component (inline-rendered, no streamed Suspense, honest copy — no fake discount unless owner confirms one) that POSTs email to a new backend endpoint apps/backend/src/api/store/subscribe/route.ts. Backend: persist the subscriber (Medusa customer or a lightweight subscribers table/module) and send a branded welcome email via the existing lib/email.ts (env-gated; no-op when RESEND_API_KEY unset), mirroring the HTML style in subscribers/order-confirmation.ts. Validate email server-side; idempotent on repeat signup. WhatsApp CTA stays dark while hasRealWhatsapp=false.
- **Key files:** apps/backend/src/lib/email.ts, apps/backend/src/api/store/contact/route.ts, apps/backend/src/subscribers/order-confirmation.ts, apps/storefront/src/modules/layout, apps/storefront/src/lib/aquora/brand.ts
- **Acceptance:** ✓ A visible newsletter signup renders inline in the storefront and submits to /store/subscribe ✓ POST /store/subscribe validates email, stores it idempotently, and (when RESEND_API_KEY set) sends a branded welcome email; returns 200 with no email when key unset ✓ No fabricated discount/scarcity in the copy ✓ Duplicate signups do not create duplicate records or re-spam ✓ Backend builds and the endpoint is reachable behind the publishable-key pattern used by other /store routes
- **🔒 Owner input:** Confirm sender identity / verified Resend domain (RESEND_FROM) and whether a welcome incentive (e.g. free delivery reminder vs a real discount code) should be included. If a discount code is wanted, owner must create the real Medusa promotion.

#### WS6-PR6 — Browse/cart abandonment recovery email job (Resend, opted-in only)  `[M/med]` 🔒 · depends: WS6-PR5
- **Problem:** Medusa carts with a captured email that never convert are pure lost demand, and there is no abandonment subscriber/job (grep for abandon/cart.updated in apps/backend/src returns nothing). The codebase already proves the env-gated daily-job pattern (apps/backend/src/jobs/review-request.ts) and Resend email pattern, so a cart-recovery job is a natural, low-risk extension.
- **Scope:** Add apps/backend/src/jobs/cart-abandonment.ts modeled on jobs/review-request.ts: find carts with an email, line items, no completed order, older than a delay window (e.g. 1h/24h) and not already nudged (mark cart metadata.abandon_email_sent for idempotency), then send a branded recovery email (reuse order-confirmation HTML style) deep-linking back to the cart/products. Env-gated via emailEnabled(); capped batch; honest copy, no fake scarcity/countdown. Email only (WhatsApp dark until real number). One or two-step cadence max.
- **Key files:** apps/backend/src/jobs/review-request.ts, apps/backend/src/lib/email.ts, apps/backend/src/subscribers/order-confirmation.ts, apps/backend/src/jobs/README.md
- **Acceptance:** ✓ cart-abandonment job runs on a schedule, no-ops when RESEND_API_KEY unset ✓ Only carts with an email, items, no order, within the window, and not previously emailed are targeted; each is marked sent (idempotent, never double-sent) ✓ Recovery email is branded, links back to recover the cart, contains no fabricated urgency/scarcity ✓ Batch is capped per run; job never throws into the scheduler ✓ Backend builds and job is registered like review-request
- **🔒 Owner input:** Confirm the abandonment delay/cadence (e.g. single 24h nudge vs 1h+24h) and that emailing carts whose email was captured at checkout is acceptable under consent expectations.

#### WS6-PR7 — Winback email job for lapsed purchasers (Resend)  `[M/med]` 🔒 · depends: WS6-PR5
- **Problem:** Post-purchase lifecycle stops at confirmation/shipped/review-request; there is no winback for customers who bought once and went quiet — a high-value, low-cost reactivation lever for a consumables-heavy catalogue (chemicals, filters). The daily env-gated job pattern (jobs/review-request.ts) already exists to copy.
- **Scope:** Add apps/backend/src/jobs/winback.ts: identify customers whose most recent order is older than N days (e.g. 90/120) with no newer order, exclude unsubscribed, and send one branded reactivation email (reuse email HTML style) surfacing relevant restock categories / the store link. Idempotent per customer per window (mark customer/order metadata winback_sent_at). Env-gated; batch-capped; honest copy; email only. Respect any unsubscribe flag from PR5's subscriber store.
- **Key files:** apps/backend/src/jobs/review-request.ts, apps/backend/src/lib/email.ts, apps/backend/src/subscribers/order-confirmation.ts
- **Acceptance:** ✓ winback job runs on schedule and no-ops without RESEND_API_KEY ✓ Targets only customers whose latest order exceeds the lapse threshold and who have a newer order=false and unsubscribed=false ✓ Each customer is emailed at most once per window (idempotent marker) ✓ Email is branded, honest (no fabricated offers), email-only ✓ Backend builds; job registered and batch-capped
- **🔒 Owner input:** Confirm the lapse threshold (e.g. 90 vs 120 days) and whether winback should carry a real, owner-created promotion code or stay offer-free.

#### WS6-PR8 — Generate Google Merchant Center + Meta product feeds  `[L/med]` 🔒
- **Problem:** There is no product feed for paid acquisition (grep for merchant/feed/catalog in apps returns only unrelated payment-trust + retail-sync). Without a Google Merchant Center / Meta catalog feed the store cannot run Shopping/PMax or Advantage+ catalog ads against its 6k SKUs — the cheapest scalable demand channel for an e-commerce funnel that is already optimized.
- **Scope:** Add a feed endpoint (e.g. apps/storefront/src/app/feeds/google.xml/route.ts and /feeds/meta.csv/route.ts, or a backend route) that paginates all products from the Medusa store API (reuse the fetchAllHandles pagination approach in sitemap.ts) and emits a valid Google RSS 2.0 product feed and a Meta-compatible CSV: id, title, description, link (/ae/products/handle), image_link, price in AED, availability, brand (from metadata.brand), gtin/mpn/sku where present, condition=new, product_type/category. Use real data only — omit availability rather than faking stock; respect the inventory-capped-at-1 reality by reporting availability honestly (in_stock when purchasable). Cache the response at the edge.
- **Key files:** apps/storefront/src/app/sitemap.ts, apps/storefront/src/modules/products/components/product-jsonld/index.tsx, apps/backend/src/lib/retail.ts, apps/storefront/src/lib/util/env.ts
- **Acceptance:** ✓ GET /feeds/google.xml returns a valid Google Shopping RSS feed covering all products with required fields (id, title, link, image_link, price AED, availability, brand) ✓ GET /feeds/meta.csv returns a Meta-catalog-compatible CSV with the same core fields ✓ Prices/availability come from real backend data; no fabricated stock — availability reflects purchasability ✓ Feed paginates the full catalogue without timing out and is edge-cacheable (cache headers set) ✓ Feed validates in Google Merchant Center's feed spec (spot-check a sample of rows)
- **🔒 Owner input:** Owner must create/connect the Google Merchant Center and Meta Commerce accounts and confirm the feed URL there; also confirm how to map condition and whether GTIN/MPN exist in product metadata for de-duplication.

---
## M5 · SMART — close the personalization loop
*Retail Recommendations model + surfaces, conversation insights, agent memory.*

#### WS5-PR1 — Provision Google Retail Recommendations model and wire RETAIL_REC_SERVING  `[M/low]` 🔒
- **Problem:** retailPredict() in apps/backend/src/lib/retail.ts short-circuits to null whenever RETAIL_REC_SERVING is empty, which it currently is. So every /store/recommend call falls back to the content heuristic and the (already-built) home/PDP 'Recommended for you' rails are never actually personalised, even though full user-event ingestion (home/detail/add-to-cart/purchase/search) is already feeding the Retail catalog. The remaining unlock is a provisioned recommendation model + serving config and the env var that points the code at it.
- **Scope:** In the Google Cloud project emerge-digital-web-7034, create a Retail 'Recommended for You' (or 'Others You May Like') recommendation model on default_catalog, create a serving config bound to it, confirm the catalog has enough ingested user events + a product catalog to train, then set the RETAIL_REC_SERVING env var on the Cloud Run service aquora-backend (image-only deploy preserves other env vars) to the serving config id. No application code change is required beyond confirming retailPredict() reads RETAIL_REC_SERVING (it does). Document the serving-config id and model type in the repo (e.g. a short note in apps/backend/README or infra notes). Verify by calling GET /store/recommend?v=<id> and confirming the response 'source' field flips from 'content' to 'retail-predict'.
- **Key files:** apps/backend/src/lib/retail.ts, apps/backend/src/api/store/recommend/route.ts, apps/storefront/src/modules/home/components/recommended-rail/index.tsx
- **Acceptance:** ✓ A Retail recommendation model + serving config exists in project emerge-digital-web-7034 on default_catalog and is in a serving/ready state ✓ RETAIL_REC_SERVING is set on the aquora-backend Cloud Run service to the serving config id ✓ GET /store/recommend?v=<visitorId> returns source:'retail-predict' (not 'content') for a visitor with event history ✓ GET /store/recommend?handle=<a real product handle> returns personalised/contextual ids and the PDP rail still renders ✓ If the model is not yet trained, the route still safely falls back to content and renders without error
- **🔒 Owner input:** Owner must create/approve the Retail Recommendations model + serving config in GCP (incurs Retail prediction cost) and confirm the serving-config id to set as RETAIL_REC_SERVING; owner also confirms the catalog has enough ingested events to train.

#### WS5-PR2 — Add a cart-aware 'Complete your setup' recommendation rail to the cart page  `[S/low]`
- **Problem:** The cart page (apps/storefront/src/modules/cart/templates/index.tsx) has no cross-sell surface, yet the cart is the highest-intent moment and the recommend infrastructure (RecommendedRail + /store/recommend with a contextHandle) already exists and is used on home + PDP. The Gemini CX plan calls for a cart 'recommended for you' surface.
- **Scope:** Render the existing RecommendedRail component near the bottom of the cart template (below the items/summary, above MobileCheckoutBar), passing the handle of the first/most-expensive cart line item so /store/recommend returns contextual complementary items ('you may also need'). Title it e.g. 'Complete your setup'. Must render INLINE (no streamed Suspense) per the deferred-suspense constraint. Only render when cart has items. Pass the shared visitorId. Do not fabricate stock/urgency. If fewer than 3 recommendations come back, the rail already self-hides.
- **Key files:** apps/storefront/src/modules/cart/templates/index.tsx, apps/storefront/src/modules/home/components/recommended-rail/index.tsx
- **Acceptance:** ✓ When the cart has items, a 'Complete your setup' rail renders below the cart contents on /[countryCode]/cart ✓ The rail passes a real cart line-item handle so /store/recommend returns contextual items, and excludes items already in the cart where the route supports it ✓ The rail is server-inline / client-mounted with NO streamed <Suspense> wrapping it ✓ When the cart is empty or fewer than 3 recs return, no rail (or empty space) is shown ✓ No fabricated reviews/stock/urgency are introduced

#### WS5-PR3 — Persist Ask-Aqua conversation memory across reloads via stored visitorId  `[M/low]`
- **Problem:** The assistant keeps multi-turn memory only in an in-component useRef (historyRef in apps/storefront/src/modules/layout/components/ai-assistant/index.tsx); a page reload or navigation wipes the conversation, so 'memory' (a stated CX-plan goal) is session-only. A stable visitorId (aq_vid) already exists and is shared with Retail — it can key durable history.
- **Scope:** Persist the assistant message history + Gemini history (last ~12 turns) to localStorage keyed by the aq_vid visitorId, and rehydrate on mount so the conversation survives reloads/navigation within the storefront. Cap stored size (e.g. last 12 turns, drop image blobs) to avoid quota issues. Add a 'Clear chat' control that resets both the UI messages and the stored history. Keep the request payload shape to /store/assistant unchanged (it already accepts history + v). Do NOT store PII beyond what the user typed; never store order numbers/emails in plaintext if avoidable (truncate or skip the support exchanges).
- **Key files:** apps/storefront/src/modules/layout/components/ai-assistant/index.tsx, apps/storefront/src/lib/aquora/retail-track.ts
- **Acceptance:** ✓ Reloading the page restores the prior Ask-Aqua conversation (messages + the model history sent to the backend) ✓ History is keyed by the existing aq_vid visitorId and capped to a bounded number of turns/size ✓ A 'Clear chat' action wipes both the on-screen messages and the persisted history ✓ Image/audio payloads are not persisted (only text turns), keeping localStorage small ✓ No change to the /store/assistant request contract

#### WS5-PR4 — Feed Ask-Aqua's own searches and views into Google Retail as user events  `[S/low]`
- **Problem:** When a shopper uses Ask-Aqua, the assistant searches the catalogue and surfaces products via Retail (searchProducts/getProductTool in apps/backend/src/api/store/assistant/route.ts), but those high-signal interactions are never written back as Retail user events — only storefront browsing (search-tracker, product-actions) is. So the recommendation model learns nothing from assistant-driven discovery, weakening personalization exactly where the AI differentiator lives.
- **Scope:** In the assistant route, after the agentic loop resolves, fire-and-forget writeUserEvent() calls reusing the existing visitorId: a 'search' event with the effective query when search_products ran, and 'detail-page-view' events for the product handles the agent surfaced in suggestions. Reuse writeUserEvent from lib/retail.ts (already imported pattern in event/route.ts); do NOT await (never block the chat response) and never write 'add-to-cart'/'purchase-complete' from here (those fire client-side on the real action to avoid double counting). Respect the existing visitorId scoping and PII rules (no event payload contains emails/order numbers).
- **Key files:** apps/backend/src/api/store/assistant/route.ts, apps/backend/src/lib/retail.ts
- **Acceptance:** ✓ When the assistant runs search_products, a Retail 'search' user event is written with the visitorId and query ✓ Product handles the assistant surfaces are written as 'detail-page-view' events (deduped within the turn) ✓ The writes are non-blocking (void/fire-and-forget) and a Retail failure never affects the chat reply ✓ No 'add-to-cart'/'purchase-complete' events are emitted from the assistant route (avoids double counting) ✓ No PII (emails/order numbers) appears in any emitted event

#### WS5-PR5 — Stand up the Ask-Aqua conversation-insights pipeline (log metrics + BigQuery sink)  `[M/low]` 🔒
- **Problem:** The assistant already emits a structured per-conversation log (the 'aqua_session' JSON in apps/backend/src/api/store/assistant/route.ts: visitorId, masked query, toolsUsed, ai, cta, nSuggestions, error). This is Phase 3's foundation, but nothing consumes it — there are no log-based metrics, no BigQuery export, and no dashboard, so intents/FAQ-gaps/agent-assisted-conversion are invisible.
- **Scope:** In GCP project emerge-digital-web-7034, create a Cloud Logging sink that routes the aqua_session entries (filter on jsonPayload.type='aqua_session' from the aquora-backend Cloud Run service) into a BigQuery dataset, plus a small set of log-based metrics (e.g. sessions/day, %ai-fallback via the 'ai' field, %with-cta go_to_checkout, error rate, tool-usage counts). Optionally add a saved BigQuery query or a Looker Studio/Cloud Monitoring dashboard for: top intents (from masked query), FAQ gaps (sessions with 0 suggestions), and the agent-assisted-conversion funnel (sessions with cta=go_to_checkout). Document the dataset + metric names in repo notes. Confirm the existing maskPII step keeps emails/order numbers out of BigQuery. No storefront change.
- **Key files:** apps/backend/src/api/store/assistant/route.ts
- **Acceptance:** ✓ A Cloud Logging→BigQuery sink exists filtering jsonPayload.type='aqua_session' from aquora-backend, and rows land in the dataset ✓ Log-based metrics exist for at least: session count, ai-fallback rate, go_to_checkout-cta rate, error rate ✓ A documented BigQuery query (or dashboard) surfaces top intents, zero-suggestion (FAQ-gap) sessions, and the cta funnel ✓ Sampled BigQuery rows contain no raw emails or order numbers (maskPII verified) ✓ Dataset/sink/metric names are recorded in repo notes
- **🔒 Owner input:** Owner must approve creating a BigQuery dataset + logging sink (storage cost) and confirm where the dashboard should live (Looker Studio vs Cloud Monitoring).

#### WS5-PR6 — Add an honest WhatsApp/email escalation handoff from Ask-Aqua (no SMS, guarded behind real number)  `[S/low]` 🔒
- **Problem:** The assistant's SYSTEM prompt already offers to connect customers to the team via email (hello@aquora.ae) or WhatsApp for anything it can't resolve, but there is no actual handoff UI in the chat, and the WhatsApp number is currently a +971...0000 placeholder (hasRealWhatsapp=false), so a WhatsApp CTA would be dark. The CX plan's 'Support' pillar needs a real, honest escalation path that doesn't fabricate a channel.
- **Scope:** When the assistant cannot resolve a request (e.g. ai:false fallback, or the reply offers a consultation/escalation), render an escalation block in the chat with an email CTA to hello@aquora.ae (always available) and a WhatsApp CTA that is shown ONLY when a real WhatsApp number is configured (reuse the existing hasRealWhatsapp / placeholder-detection used elsewhere in the storefront) — otherwise hide the WhatsApp button entirely. Email/WhatsApp only, never SMS. Optionally add a backend hint (e.g. cta type 'escalate') so the UI knows when to show it. Must render inline (no streamed Suspense). Pre-fill the email/WhatsApp message with a short context summary (no PII beyond what the user typed).
- **Key files:** apps/storefront/src/modules/layout/components/ai-assistant/index.tsx, apps/backend/src/api/store/assistant/route.ts
- **Acceptance:** ✓ On an unresolved/fallback reply, the chat shows an escalation block with an email CTA to hello@aquora.ae ✓ The WhatsApp CTA appears only when a real WhatsApp number is configured (hasRealWhatsapp true); with the placeholder it is hidden, not dark ✓ No SMS option is offered anywhere ✓ The escalation block renders inline (no streamed <Suspense>) ✓ Pre-filled email/WhatsApp text contains no PII beyond the user's own typed message
- **🔒 Owner input:** Owner must provide the real Aquora WhatsApp number (replacing the +971...0000 placeholder) for the WhatsApp CTA to activate; the email path works without it.

---
## Quick-wins track (interleave after M0 — small, low-risk, no owner gate)
*Finishes the started conversion work; mergeable any time once the harness exists.*

#### WS2-PR1 — Optimistic cart line removal with revert-on-error  `[S/low]`
- **Problem:** DeleteButton (apps/storefront/src/modules/common/components/delete-button/index.tsx) sets isDeleting then awaits deleteLineItem, but the row only disappears after the server round-trip + Next revalidation lands — so removing a line feels laggy (the same gap optimistic qty already fixed for quantity in the cart item). Cart quantity is already optimistic; removal is not, leaving the funnel half-optimized.
- **Scope:** Make line removal feel instant: when delete is tapped, optimistically hide/collapse the line (fade + reduce to height 0, or hand an onOptimisticRemove signal up to the Item row) before the await resolves, and revert (restore the row + show the existing rose error message) if deleteLineItem rejects. Keep the spinner state for in-flight feedback. Do NOT introduce a streamed Suspense around the cart list — the cart grid must remain inline-rendered. Wire the optimistic-remove state through DeleteButton (passed an optional onRemoving/onRemoved callback) or lift a removing flag into Item (apps/storefront/src/modules/cart/components/item/index.tsx) so the <Table.Row> can animate out. Preserve the existing error-recovery path already in delete-button.
- **Key files:** apps/storefront/src/modules/common/components/delete-button/index.tsx, apps/storefront/src/modules/cart/components/item/index.tsx, apps/storefront/src/modules/cart/templates/items.tsx
- **Acceptance:** ✓ Tapping remove on a cart line visually collapses/fades that row immediately (before the network call resolves), not after revalidation. ✓ If deleteLineItem rejects, the row is restored and the existing rose 'Couldn’t remove this — please try again.' message shows. ✓ The cart list still renders inline (no streamed <Suspense> wrapping the items); add-to-cart, cart count, and the grid stay visible. ✓ Spinner/disabled state still prevents double-submit while the delete is in flight. ✓ No fabricated data introduced; behavior is purely UI timing.

#### WS2-PR2 — Per-field inline validation across the shipping & billing address forms  `[M/low]`
- **Problem:** Only the email field in ShippingAddress (apps/storefront/src/modules/checkout/components/shipping-address/index.tsx) uses the new Input `error` prop for inline validation; required fields (first name, last name, address_1, city, country, UAE emirate, plus billing_address fields) still fall back to the browser tooltip on submit. The Input component already supports an `error` string (apps/storefront/src/modules/common/components/input/index.tsx) — the wiring just isn’t there for the rest of the form, a known checkout-friction item.
- **Scope:** Add on-blur per-field validation for the required address fields, reusing the Input `error` prop (rose border + message) exactly like the existing email validator. Validate non-emptiness for first_name, last_name, address_1, city, country_code, and province/emirate (when shown); keep phone optional. Surface a clear message (e.g. 'First name is required.'). Re-validate a field on change only after it has first errored (match the email pattern already in the file). Apply the same treatment to the billing address form (apps/storefront/src/modules/checkout/components/billing_address/index.tsx), which already imports the same Input. Do not add a streamed Suspense; the form must stay inline. Keep native `required` as the hard submit gate — inline messages are additive.
- **Key files:** apps/storefront/src/modules/checkout/components/shipping-address/index.tsx, apps/storefront/src/modules/checkout/components/billing_address/index.tsx, apps/storefront/src/modules/common/components/input/index.tsx
- **Acceptance:** ✓ Blurring an empty required field (first/last name, address, city, country, emirate/province when shown) shows an inline rose message and border via the Input `error` prop. ✓ An errored field clears its message as soon as it becomes valid on subsequent input. ✓ Email validation behavior is unchanged (no regression). ✓ Phone and company remain optional and never show a required error. ✓ Billing address form gets the same inline validation, reusing the Input `error` prop. ✓ Submit is still ultimately gated by native required; no streamed Suspense added.

#### WS2-PR3 — PDP option-level stock & price hints on the variant selector  `[M/low]`
- **Problem:** OptionSelect (apps/storefront/src/modules/products/components/product-actions/option-select.tsx) renders bare option buttons with no signal that a given option leads to an out-of-stock or differently-priced variant. Shoppers can pick a model, then discover at the Add-to-cart button that it’s out of stock — friction the PDP already partly addresses with the selected-variant stock line in product-actions/index.tsx.
- **Scope:** Pass real variant data into OptionSelect so each option value can show an HONEST hint derived only from actual variant inventory/price: dim/disable an option value whose every matching variant is out of stock (manage_inventory && inventory_quantity<=0 && !allow_backorder), and optionally show a price delta where variants legitimately differ. Compute these from product.variants in product-actions/index.tsx and pass a per-value map into OptionSelect. No fabricated stock, urgency, or scarcity — only render a hint when the underlying variant data supports it; if data is ambiguous, render nothing. Keep everything inline (no streamed Suspense). Must not regret the single-variant preselect / isValidVariant logic already in index.tsx.
- **Key files:** apps/storefront/src/modules/products/components/product-actions/option-select.tsx, apps/storefront/src/modules/products/components/product-actions/index.tsx
- **Acceptance:** ✓ An option value whose matching variant(s) are all genuinely out of stock is visibly disabled/dimmed (derived from real inventory fields, never hardcoded). ✓ Where variants legitimately differ in price, the option may show a real price hint; otherwise no price text is shown. ✓ No fabricated stock counts, scarcity, or urgency copy is introduced. ✓ Existing variant selection, single-variant preselect, and Add-to-cart enable/disable logic still work. ✓ Rendering stays inline; no streamed <Suspense> added around the actions.

#### WS2-PR4 — Email-my-cart abandonment recovery (Resend, env-gated)  `[M/med]` 🔒
- **Problem:** There is no way for a shopper to save or be reminded of their cart. The backend already has a no-op-safe Resend client (apps/backend/src/lib/email.ts: sendEmail/emailEnabled) and a clean public store-route template (apps/backend/src/api/store/contact/route.ts), but no cart-recovery endpoint or storefront entry point exists. Comms policy is Email + WhatsApp ONLY (no SMS).
- **Scope:** Add a public backend store route (e.g. apps/backend/src/api/store/cart-email/route.ts) that accepts { email, cart_id } (or a serialized line summary), validates the email like the contact route, and — env-gated on emailEnabled() — sends the shopper a branded HTML email containing their current cart lines + a deep link back to the cart/checkout, using sendEmail. No-op-safe (return ok + log when RESEND_API_KEY unset). On the storefront, add a lightweight 'Email my cart' action in the cart summary (apps/storefront/src/modules/cart/templates/summary.tsx) that collects an email (reuse the Input `error` prop for validation) and POSTs to the route. Render inline (no streamed Suspense). Do NOT fabricate discounts or urgency; this is a pure save/remind utility. No SMS path.
- **Key files:** apps/backend/src/api/store/cart-email/route.ts, apps/backend/src/lib/email.ts, apps/backend/src/api/store/contact/route.ts, apps/storefront/src/modules/cart/templates/summary.tsx, apps/storefront/src/modules/common/components/input/index.tsx
- **Acceptance:** ✓ POST to the new route with a valid email and a cart returns ok and (when RESEND_API_KEY is set) sends an email listing the real cart lines + a working link back to the cart. ✓ With RESEND_API_KEY unset, the route logs and returns ok (no crash, no broken UI) — matching the contact route’s no-op-safe pattern. ✓ Invalid email returns 400 with a clear message; the storefront shows the inline Input error. ✓ The 'Email my cart' control renders inline in the cart summary and never streams behind a Suspense. ✓ Email content uses only real cart data — no fabricated discounts, scarcity, or urgency; no SMS channel.
- **🔒 Owner input:** Confirm RESEND_API_KEY, RESEND_FROM (verified domain), and the sender/from address for cart emails; confirm the from-name/branding and that hello@aquora.ae (or similar) is acceptable as the reply-to. Email actually sends only after these secrets are set by the owner.

#### WS2-PR5 — Out-of-stock 'email me when available' capture on the PDP  `[M/med]` 🔒 · depends: WS2-PR4
- **Problem:** When the selected variant is out of stock, product-actions (apps/storefront/src/modules/products/components/product-actions/index.tsx) only shows a disabled 'Out of stock' button — the buyer’s intent is lost entirely. There’s no honest, no-fabrication way to recover that demand. The Resend client + store-route pattern already exist (apps/backend/src/lib/email.ts, apps/backend/src/api/store/contact/route.ts).
- **Scope:** When the selected variant is genuinely out of stock (inStock===false in product-actions), replace/augment the disabled button area with an inline 'Email me when it’s back' field (reuse Input `error` prop for validation). Add a public backend store route (e.g. apps/backend/src/api/store/notify-restock/route.ts) accepting { email, variant_id, product_handle } that validates email and — env-gated — emails the team a restock-notify request via sendEmail (and/or persists it), no-op-safe when email is unprovisioned. This is intent capture only: do NOT promise a date or fabricate stock. Render inline (no streamed Suspense). Email + WhatsApp only — no SMS. Must not interfere with the existing in-stock Add-to-cart/quantity UI.
- **Key files:** apps/storefront/src/modules/products/components/product-actions/index.tsx, apps/backend/src/api/store/notify-restock/route.ts, apps/backend/src/lib/email.ts, apps/storefront/src/modules/common/components/input/index.tsx
- **Acceptance:** ✓ On an out-of-stock variant, an inline 'Email me when available' field appears in place of (or beside) the disabled button and validates the email via the Input error prop. ✓ Submitting POSTs { email, variant_id, product_handle } to the new route, which returns ok; with email provisioned it notifies the team via Resend, and is no-op-safe when unprovisioned. ✓ No restock date is promised and no stock figure is fabricated. ✓ In-stock products are unaffected (Add-to-cart, quantity stepper, stock line unchanged). ✓ The capture UI renders inline (no streamed Suspense); no SMS channel used.
- **🔒 Owner input:** Confirm where restock requests should go: team notification email address and/or whether to persist them (a DB table/model) for later batch notify; same Resend secrets as PR4. Decide whether a future automated 'back in stock' email is in scope (likely a separate PR).

#### WS2-PR6 — Pre-address express wallet (Apple/Google Pay) that collects & applies its own address  `[L/high]` 🔒
- **Problem:** ExpressCheckout (apps/storefront/src/modules/checkout/components/express-checkout/index.tsx) only mounts on the payment step AFTER address collection (gated on isOpen in apps/storefront/src/modules/checkout/components/payment/index.tsx), so the one-tap wallet win is buried at the end of the funnel. The owner-flagged R2/R3 goal is to surface it earlier (and on the cart), but that means the wallet must collect shipping/billing/email FROM the wallet sheet and apply them to the Medusa cart before placeOrder — the explicitly 'risky' part to spec carefully.
- **Scope:** Rework a copy of the express flow so it can run pre-address: request name/email/shipping address via the Stripe Express Checkout Element’s shipping/contact collection options, then in onConfirm map the wallet-provided address+email onto the Medusa cart (setAddresses/updateCart) and select a shipping method BEFORE initiating the payment session and placeOrder. Keep the existing hard safety invariant: mount its OWN isolated <Elements> in deferred mode so a wallet failure can never white-screen checkout or affect the card path (preserve the comment’s rationale). Surface this pre-address express button at the top of the checkout flow and/or on the cart, behind the same onReady availability gate (render nothing until a wallet is available). Render inline — never behind a streamed Suspense. The post-address express on the payment step (PR-shipped) must keep working as a fallback. Ship behind a flag if the address-application path can’t be fully verified.
- **Key files:** apps/storefront/src/modules/checkout/components/express-checkout/index.tsx, apps/storefront/src/modules/checkout/components/payment/index.tsx, apps/storefront/src/modules/cart/templates/summary.tsx, apps/storefront/src/lib/data/cart.ts
- **Acceptance:** ✓ A pre-address express (Apple/Google Pay) button appears earlier in the funnel (checkout top and/or cart) only when a wallet is actually available (onReady gate). ✓ On confirm, the wallet-provided shipping address + email + name are applied to the Medusa cart and a shipping method is selected before payment is initiated and the order is placed. ✓ The wallet still mounts its OWN isolated deferred <Elements>; any wallet failure cannot white-screen checkout or break the card path (isolation invariant preserved). ✓ The existing post-address express on the payment step continues to work unchanged. ✓ All express UI renders inline (no streamed Suspense); below Stripe minimum or no key still renders nothing without crashing. ✓ If the address-application path cannot be fully verified, the pre-address surface ships behind a flag defaulting off.
- **🔒 Owner input:** Confirm shipping-method selection rule when address is wallet-supplied (single flat UAE rate? auto-pick cheapest?), that Apple Pay domain registration covers the cart/checkout-top surfaces, and acceptance of the address-collection UX. Owner should sanity-check on a real device before enabling pre-address by default.